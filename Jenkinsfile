node {

  stage ('init') {
   echo "Initialize"
  }

  stage ('compile') {
    echo "build this repos bbbb"
  }
  
  stage ('test') {
    echo "test"
  } 
  
   stage ('publish') {
    echo "publish"
  }
  
}
