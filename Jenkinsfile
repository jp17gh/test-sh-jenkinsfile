node {

  for ( e in env ){
        echo  " e is + ${e} " 
  }
  
  stage ('init') {
   echo "Initialize"
  }

  stage ('compile') {
    echo "Compile this repos bbbb"
  }
  
  stage ('test stage') {
    echo "test"
  } 
  
   stage ('publish') {
    echo "publish "
  }
}
