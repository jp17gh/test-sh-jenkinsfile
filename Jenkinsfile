node {

  stage ('init') {
   echo "Initialize"
  }

  stage ('build') {
    echo "build this repos"
  }
  
  stage ('test') {
    echo "test"
  } 
  
   stage ('publish') {
    echo "publish"
  }
  
}
